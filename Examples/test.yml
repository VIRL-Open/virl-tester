config:
  # the VIRL host
  host: 172.23.175.92
  # username and password
  user: guest
  password: guest
  # loglevel (0-4, 4=Debug)
  loglevel: 2
  # max parallel simulations
  parallel: 1
  # default wait time (spinup / packet capture)
  wait: 300


includes:
  - Examples/command-usecase2.yml

